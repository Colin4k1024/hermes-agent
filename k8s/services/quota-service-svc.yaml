apiVersion: v1
kind: Service
metadata:
  name: quota-service
  namespace: hermes-platform
  labels:
    app: hermes
    tier: backend
    component: quota-service
spec:
  type: ClusterIP
  ports:
    - port: 8003
      targetPort: 8003
      protocol: TCP
      name: http
  selector:
    app: hermes
    component: quota-service
