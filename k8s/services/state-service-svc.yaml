apiVersion: v1
kind: Service
metadata:
  name: state-service
  namespace: hermes-platform
  labels:
    app: hermes
    tier: backend
    component: state-service
spec:
  type: ClusterIP
  ports:
    - port: 8006
      targetPort: 8006
      protocol: TCP
      name: http
  selector:
    app: hermes
    component: state-service
