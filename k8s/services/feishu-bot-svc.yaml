apiVersion: v1
kind: Service
metadata:
  name: feishu-bot
  namespace: hermes-platform
  labels:
    app: hermes
    tier: backend
    component: feishu-bot
spec:
  type: ClusterIP
  ports:
    - port: 8005
      targetPort: 8005
      protocol: TCP
      name: http
  selector:
    app: hermes
    component: feishu-bot
