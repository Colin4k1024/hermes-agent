apiVersion: v1
kind: Service
metadata:
  name: skills-registry
  namespace: hermes-platform
  labels:
    app: hermes
    tier: backend
    component: skills-registry
spec:
  type: ClusterIP
  ports:
    - port: 8004
      targetPort: 8004
      protocol: TCP
      name: http
  selector:
    app: hermes
    component: skills-registry
